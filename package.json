{
  "name": "root",
  "private": true,
  "scripts": {
    "deploy:stage": "git push stage develop:master",
    "deploy:prod": "git push prod master",
    "preinstall": "printenv | egrep \"NVM|NPM\"",
    "preheroku-prebuild": "printenv | egrep \"NVM|NPM\"",
    "heroku-prebuild": "npm run install:ci --workspace=landing  --workspace=react-projects",
    "heroku-postbuild": "npm run clean --workspace=landing  --workspace=react-projects",
    "build": "npm run build:ci --workspace=landing  --workspace=react-projects"
  },
  "engines": {
    "node": "16.14.x",
    "npm": "8.3.x"
  },
  "author": "Nicolas Ramirez",
  "license": "ISC",
  "workspaces": [
    "packages/server",
    "packages/landing",
    "packages/react-projects"
  ]
}
